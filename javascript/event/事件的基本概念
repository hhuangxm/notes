我的思想可能比较传统，觉得学习任何东西，都要从基本概念学起，但是可能看基本概念又看不懂，所以会结合一些例子吧。

事件处理这一章是昨晚开始看的，只是想先看一遍大概，毕竟看一次肯定不能全懂，可能看了很多遍也不能全懂，但是贵在坚持学习探讨。


（1） 事件
  定义：在这种程序设计风格下，当文档、浏览器、元素或与之相关的对象发生某些有趣的事情时，Web浏览器就会产生事件（event）。
  事件本身并不是一个需要定义的技术名词。简而言之，事件就是Web浏览器通知应用程序发生了什么事情。事件不是JavaScript对象，不会出现在程序源码中。
  
（2）事件类型
  定义：事件类型（event type）是一个用来说明发生什么类型事件的字符串。
  例如：“mouseover”表示用户移动鼠标
  
（3）事件目标
  定义：事件目标（event target）是发生的事件或与之相关的对象。
  例如：window上的load事件或<button>上的click事件。
  常见的事件目标：Window, Document, Element
  
（4）事件处理程序（事件监听程序）
   定义：事件处理程序（event handler）或事件监听程序（event listener）是处理或响应事件额函数。
  
（5）事件对象
  定义：事件对象（event object）是与特定事件相关且包含有关该事件详细信息的对象。
  例如：
  var b = document.getElementById("myButton");
  b.addEventListener("click", function(event){
    console.log(event);
  },false);
  
  上面代码中的event就是事件对象。它包含了事件相关的详细信息。
  
（6）事件传播
  定义：事件传播（event propagation）是浏览器决定哪个对象触发其事件处理程序的过程。
  这个看的不太懂，事件传播嘛，理解的有两个，就是事件冒泡和事件捕获，理解是对的，但是不全面。以后再做补充。
  

 
